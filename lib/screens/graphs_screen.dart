import 'package:flutter/material.dart';

// This screen is a screen under home_screen

class GraphsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text('Graphs screen placeholder'),
    );
  }
}
